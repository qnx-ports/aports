diff --color -U 6 -r before/configure after/configure
--- before/configure	2026-04-02 11:41:59.166082557 -0400
+++ after/configure	2026-04-02 11:41:19.425688912 -0400
@@ -7543,13 +7543,13 @@
 then
 	LINKCC='$(PURIFY) $(CC)'
 	case $ac_sys_system in
 	QNX*)
 	   # qcc must be used because the other compilers do not
 	   # support -N.
-	   LINKCC=qcc;;
+	   LINKCC=gcc;;
 	esac
 fi
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $LINKCC" >&5
 printf "%s\n" "$LINKCC" >&6; }
 
 # EXPORTSYMS holds the list of exported symbols for AIX.
@@ -7734,12 +7734,22 @@
 
       if test "$with_pydebug" != yes
       then
         PY3LIBRARY=libpython3.so
       fi
       ;;
+    QNX)
+      LDLIBRARY='libpython$(LDVERSION).so'
+      BLDLIBRARY='-L. -lpython$(LDVERSION)'
+      RUNSHARED=LD_LIBRARY_PATH=`pwd`${LD_LIBRARY_PATH:+:${LD_LIBRARY_PATH}}
+
+      if test "$with_pydebug" != yes
+      then
+        PY3LIBRARY=libpython3.so
+      fi
+      ;;
     hp*|HP*)
       case `uname -m` in
         ia64)
           LDLIBRARY='libpython$(LDVERSION).so'
           ;;
         *)
@@ -13754,13 +13764,13 @@
 	QNX*)
 		# -Wl,-E causes the symbols to be added to the dynamic
 		# symbol table so that they can be found when a module
 		# is loaded.  -N 2048K causes the stack size to be set
 		# to 2048 kilobytes so that the stack doesn't overflow
 		# when running test_compile.py.
-		LINKFORSHARED='-Wl,-E -N 2048K';;
+		LINKFORSHARED='-Wl,-export-dynamic -Wl,-E -Wl,--stack,8388608';;
 	VxWorks*)
 		LINKFORSHARED='-Wl,-export-dynamic';;
 	esac
 fi
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: $LINKFORSHARED" >&5
 printf "%s\n" "$LINKFORSHARED" >&6; }
diff --color -U 6 -r before/Include/internal/pycore_time.h after/Include/internal/pycore_time.h
--- before/Include/internal/pycore_time.h	2026-04-02 11:41:59.070081605 -0400
+++ after/Include/internal/pycore_time.h	2026-04-02 11:41:18.917683880 -0400
@@ -56,13 +56,13 @@
 #ifndef Py_BUILD_CORE
 #  error "this header requires Py_BUILD_CORE define"
 #endif
 
 #include "pycore_runtime_structs.h" // _PyTimeFraction
 
-#ifdef __clang__
+#if defined(__clang__) || defined(__QNX__)
 struct timeval;
 #endif
 
 #define _SIZEOF_PYTIME_T 8
 
 typedef enum {
diff --color -U 6 -r before/Lib/asyncio/unix_events.py after/Lib/asyncio/unix_events.py
--- before/Lib/asyncio/unix_events.py	2026-04-02 11:41:59.078081685 -0400
+++ after/Lib/asyncio/unix_events.py	2026-04-02 11:41:18.933684038 -0400
@@ -115,13 +115,14 @@
             # Register a dummy signal handler to ask Python to write the signal
             # number in the wakeup file descriptor. _process_self_data() will
             # read signal numbers from this file descriptor to handle signals.
             signal.signal(sig, _sighandler_noop)
 
             # Set SA_RESTART to limit EINTR occurrences.
-            signal.siginterrupt(sig, False)
+            # Doesn't work on QNX tho
+            #signal.siginterrupt(sig, False)
         except OSError as exc:
             del self._signal_handlers[sig]
             if not self._signal_handlers:
                 try:
                     signal.set_wakeup_fd(-1)
                 except (ValueError, OSError) as nexc:
diff --color -U 6 -r before/Lib/getpass.py after/Lib/getpass.py
--- before/Lib/getpass.py	2026-04-02 11:41:59.074081644 -0400
+++ after/Lib/getpass.py	2026-04-02 11:44:10.379382680 -0400
@@ -83,12 +83,14 @@
                     passwd = _raw_input(prompt, stream, input=input,
                                         echo_char=echo_char)
 
                 finally:
                     termios.tcsetattr(fd, tcsetattr_flags, old)
                     stream.flush()  # issue7208
+                    if sys.platform.startswith('qnx'):
+                        stream.seek(0, 1)
             except termios.error:
                 if passwd is not None:
                     # _raw_input succeeded.  The final tcsetattr failed.  Reraise
                     # instead of leaving the terminal in an unknown state.
                     raise
                 # We can't control the tty or stdin.  Give up and use normal IO.
diff --color -U 6 -r before/Lib/shutil.py after/Lib/shutil.py
--- before/Lib/shutil.py	2026-04-02 11:41:59.074081644 -0400
+++ after/Lib/shutil.py	2026-04-02 11:41:18.965684355 -0400
@@ -759,13 +759,13 @@
         if orig_entry is None:
             orig_st = os.lstat(name, dir_fd=dirfd)
         else:
             orig_st = orig_entry.stat(follow_symlinks=False)
 
         func = os.open  # For error reporting.
-        topfd = os.open(name, os.O_RDONLY | os.O_NONBLOCK, dir_fd=dirfd)
+        topfd = os.open(name, os.O_RDONLY | os.O_NONBLOCK | os.O_DIRECTORY, dir_fd=dirfd)
 
         func = os.path.islink  # For error reporting.
         try:
             if not os.path.samestat(orig_st, os.fstat(topfd)):
                 # Symlinks to directories are forbidden, see GH-46010.
                 raise OSError("Cannot call rmtree on a symbolic link")
diff --color -U 6 -r before/Lib/test/support/os_helper.py after/Lib/test/support/os_helper.py
--- before/Lib/test/support/os_helper.py	2026-04-02 11:41:59.130082201 -0400
+++ after/Lib/test/support/os_helper.py	2026-04-02 11:44:57.631851029 -0400
@@ -47,13 +47,13 @@
         else:
             print('WARNING: The filename %r CAN be encoded by the filesystem '
                   'encoding (%s). Unicode filename tests may not be effective'
                   % (TESTFN_UNENCODABLE, sys.getfilesystemencoding()))
             TESTFN_UNENCODABLE = None
 # Apple and Emscripten deny unencodable filenames (invalid utf-8)
-elif not support.is_apple and sys.platform not in {"emscripten", "wasi"}:
+elif not support.is_apple and (sys.platform not in {"emscripten", "wasi"} or sys.platform.startswith('qnx')):
     try:
         # ascii and utf-8 cannot encode the byte 0xff
         b'\xff'.decode(sys.getfilesystemencoding())
     except UnicodeDecodeError:
         # 0xff will be encoded using the surrogate character u+DCFF
         TESTFN_UNENCODABLE = TESTFN_ASCII \
diff --color -U 6 -r before/Modules/faulthandler.c after/Modules/faulthandler.c
--- before/Modules/faulthandler.c	2026-04-02 11:41:59.178082677 -0400
+++ after/Modules/faulthandler.c	2026-04-02 11:41:19.297687644 -0400
@@ -849,13 +849,15 @@
     struct sigaction action;
     action.sa_handler = faulthandler_user;
     sigemptyset(&action.sa_mask);
     /* if the signal is received while the kernel is executing a system
        call, try to restart the system call instead of interrupting it and
        return EINTR. */
+#ifndef __QNX__
     action.sa_flags = SA_RESTART;
+#endif
     if (chain) {
         /* do not prevent the signal from being received from within its
            own signal handler */
         action.sa_flags = SA_NODEFER;
     }
 #ifdef FAULTHANDLER_USE_ALT_STACK
diff --color -U 6 -r before/Modules/_localemodule.c after/Modules/_localemodule.c
--- before/Modules/_localemodule.c	2026-04-02 11:41:59.178082677 -0400
+++ after/Modules/_localemodule.c	2026-04-02 11:41:19.257687248 -0400
@@ -560,37 +560,37 @@
     LANGINFO(PM_STR, LC_TIME),
 
     /* The following constants are available only with XPG4, but...
        OpenBSD doesn't have CODESET but has T_FMT_AMPM, and doesn't have
        a few of the others.
        Solution: ifdef-test them all. */
-#ifdef CODESET
+#if defined(CODESET) || defined(__QNX__)
     LANGINFO(CODESET, LC_CTYPE),
 #endif
-#ifdef T_FMT_AMPM
+#if defined(T_FMT_AMPM) || defined(__QNX__)
     LANGINFO(T_FMT_AMPM, LC_TIME),
 #endif
-#ifdef ERA
+#if defined(ERA) || defined(__QNX__)
     LANGINFO(ERA, LC_TIME),
 #endif
-#ifdef ERA_D_FMT
+#if defined(ERA_D_FMT) || defined(__QNX__)
     LANGINFO(ERA_D_FMT, LC_TIME),
 #endif
-#ifdef ERA_D_T_FMT
+#if defined(ERA_D_T_FMT) || defined(__QNX__)
     LANGINFO(ERA_D_T_FMT, LC_TIME),
 #endif
-#ifdef ERA_T_FMT
+#if defined(ERA_T_FMT) || defined(__QNX__)
     LANGINFO(ERA_T_FMT, LC_TIME),
 #endif
-#ifdef ALT_DIGITS
+#if defined(ALT_DIGITS) || defined(__QNX__)
     LANGINFO(ALT_DIGITS, LC_TIME),
 #endif
-#ifdef YESEXPR
+#if defined(YESEXPR) || defined(__QNX__)
     LANGINFO(YESEXPR, LC_MESSAGES),
 #endif
-#ifdef NOEXPR
+#if defined(NOEXPR) || defined(__QNX__)
     LANGINFO(NOEXPR, LC_MESSAGES),
 #endif
 #ifdef _DATE_FMT
     /* This is not available in all glibc versions that have CODESET. */
     LANGINFO(_DATE_FMT, LC_TIME),
 #endif
diff --color -U 6 -r before/Modules/posixmodule.c after/Modules/posixmodule.c
--- before/Modules/posixmodule.c	2026-04-02 11:41:59.182082716 -0400
+++ after/Modules/posixmodule.c	2026-04-02 11:41:19.329687961 -0400
@@ -603,12 +603,18 @@
 #else
 #  define INITFUNC PyInit_posix
 #  define MODNAME "posix"
 #  define MODNAME_OBJ &_Py_ID(posix)
 #endif
 
+#ifdef __QNX__
+#  include <unix.h>
+#  include <termios.h>
+#  include <sys/ioctl.h>
+#endif
+
 /*[clinic input]
 # one of the few times we lie about this name!
 module os
 [clinic start generated code]*/
 /*[clinic end generated code: output=da39a3ee5e6b4b0d input=94a0f0f978acae17]*/
 
@@ -11153,12 +11159,17 @@
 #ifdef MS_WINDOWS
         fd = _wopen(path->wide, flags, mode);
 #else
 #ifdef HAVE_OPENAT
         if (dir_fd != DEFAULT_DIR_FD) {
             if (HAVE_OPENAT_RUNTIME) {
+#ifdef __QNX__
+		struct stat path_stat = {};
+		fstatat(dir_fd, path->narrow, &path_stat, 0);
+		if(S_ISDIR(path_stat.st_mode)) flags |= O_DIRECTORY;
+#endif
                 fd = openat(dir_fd, path->narrow, flags, mode);
 
             } else {
                 openat_unavailable = 1;
                 fd = -1;
             }
diff --color -U 6 -r before/Modules/selectmodule.c after/Modules/selectmodule.c
--- before/Modules/selectmodule.c	2026-04-02 11:41:59.182082716 -0400
+++ after/Modules/selectmodule.c	2026-04-02 11:41:19.333688001 -0400
@@ -34,12 +34,16 @@
     /* Perform runtime testing for a broken poll on OSX to make it easier
      * to use the same binary on multiple releases of the OS.
      */
 #undef HAVE_BROKEN_POLL
 #endif
 
+#ifdef __QNX__
+#  include <sys/select.h>
+#endif
+
 /* Windows #defines FD_SETSIZE to 64 if FD_SETSIZE isn't already defined.
    64 is too small (too many people have bumped into that limit).
    Here we boost it.
    Users who want even more than the boosted limit should #define
    FD_SETSIZE higher before this; e.g., via compiler /D switch.
 */
diff --color -U 6 -r before/Modules/termios.c after/Modules/termios.c
--- before/Modules/termios.c	2026-04-02 11:41:59.182082716 -0400
+++ after/Modules/termios.c	2026-04-02 11:41:19.337688040 -0400
@@ -8,13 +8,15 @@
 #endif
 
 #include "Python.h"
 
 #include <string.h>               // memset()
 #include <sys/ioctl.h>
-#include <termios.h>
+#ifndef __QNX__
+#  include <termios.h>
+#endif
 #include <unistd.h>               // _POSIX_VDISABLE
 
 // On QNX 6, struct termio must be declared by including sys/termio.h
 // if TCGETA, TCSETA, TCSETAW, or TCSETAF are used. sys/termio.h must
 // be included before termios.h or it will generate an error.
 #if defined(HAVE_SYS_TERMIO_H) && !defined(__hpux)
diff --color -U 6 -r before/Modules/_testinternalcapi/pytime.c after/Modules/_testinternalcapi/pytime.c
--- before/Modules/_testinternalcapi/pytime.c	2026-04-02 11:41:59.190082795 -0400
+++ after/Modules/_testinternalcapi/pytime.c	2026-04-02 11:41:19.297687644 -0400
@@ -1,10 +1,13 @@
 /* Test pycore_time.h */
 
 #include "parts.h"
 
+#ifdef __QNX__
+#  include <sys/select.h>
+#endif
 #include "pycore_time.h"          // _PyTime_FromSeconds()
 
 #ifdef MS_WINDOWS
 #  include <winsock2.h>           // struct timeval
 #endif
 
diff --color -U 6 -r before/Objects/mimalloc/prim/unix/prim.c after/Objects/mimalloc/prim/unix/prim.c
--- before/Objects/mimalloc/prim/unix/prim.c	2026-04-02 11:41:59.202082913 -0400
+++ after/Objects/mimalloc/prim/unix/prim.c	2026-04-02 11:41:19.353688198 -0400
@@ -48,13 +48,13 @@
   #include <sys/cpuset.h>
   #include <sys/domainset.h>
   #endif
   #include <sys/sysctl.h>
 #endif
 
-#if !defined(__HAIKU__) && !defined(__APPLE__) && !defined(__CYGWIN__) && !defined(_AIX) && !defined(__OpenBSD__) && !defined(__FreeBSD__) && !defined(__sun) && !defined(__NetBSD__)
+#if !defined(__HAIKU__) && !defined(__APPLE__) && !defined(__CYGWIN__) && !defined(_AIX) && !defined(__OpenBSD__) && !defined(__FreeBSD__) && !defined(__sun) && !defined(__NetBSD__) && !defined(__QNX__)
   #define MI_HAS_SYSCALL_H
   #include <sys/syscall.h>
 #endif
 
 //------------------------------------------------------------------------------------
 // Use syscalls for some primitives to allow for libraries that override open/read/close etc.
@@ -153,12 +153,14 @@
 // mmap
 //---------------------------------------------
 
 static int unix_madvise(void* addr, size_t size, int advice) {
   #if defined(__sun)
   return madvise((caddr_t)addr, size, advice);  // Solaris needs cast (issue #520)
+  #elif defined(__QNX__)
+  return posix_madvise(addr, size, advice);
   #else
   return madvise(addr, size, advice);
   #endif
 }
 
 static void* unix_mmap_prim(void* addr, size_t size, size_t try_alignment, int protect_flags, int flags, int fd) {
@@ -367,13 +369,17 @@
   return err;
 }
 
 int _mi_prim_decommit(void* start, size_t size, bool* needs_recommit) {
   int err = 0;
   // decommit: use MADV_DONTNEED as it decreases rss immediately (unlike MADV_FREE)
+  #ifdef __QNX__
+  err = unix_madvise(start, size, POSIX_MADV_DONTNEED);
+  #else
   err = unix_madvise(start, size, MADV_DONTNEED);
+  #endif
   #if !MI_DEBUG && !MI_SECURE
     *needs_recommit = false;
   #else
     *needs_recommit = true;
     mprotect(start, size, PROT_NONE);
   #endif
@@ -399,12 +405,14 @@
   while ((err = unix_madvise(start, size, oadvice)) != 0 && errno == EAGAIN) { errno = 0;  };
   if (err != 0 && errno == EINVAL && oadvice == MADV_FREE) {
     // if MADV_FREE is not supported, fall back to MADV_DONTNEED from now on
     mi_atomic_store_release(&advice, (size_t)MADV_DONTNEED);
     err = unix_madvise(start, size, MADV_DONTNEED);
   }
+  #elif defined(__QNX__)
+  int err = unix_madvise(start, size, POSIX_MADV_DONTNEED);
   #else
   int err = unix_madvise(start, size, MADV_DONTNEED);
   #endif
   return err;
 }
 
diff --color -U 6 -r before/Python/fileutils.c after/Python/fileutils.c
--- before/Python/fileutils.c	2026-04-02 11:41:59.218083072 -0400
+++ after/Python/fileutils.c	2026-04-02 11:41:19.401688674 -0400
@@ -1640,12 +1640,17 @@
             Py_DECREF(pathname_obj);
             return -1;
         }
 
         do {
             Py_BEGIN_ALLOW_THREADS
+            #ifdef __QNX__
+            struct stat path_stat = {};
+            stat(pathname, &path_stat);
+            if(S_ISDIR(path_stat.st_mode)) flags |= O_DIRECTORY;
+            #endif
             fd = open(pathname, flags);
             Py_END_ALLOW_THREADS
         } while (fd < 0
                  && errno == EINTR && !(async_err = PyErr_CheckSignals()));
         if (async_err) {
             Py_DECREF(pathname_obj);
@@ -1656,12 +1661,17 @@
             Py_DECREF(pathname_obj);
             return -1;
         }
         Py_DECREF(pathname_obj);
     }
     else {
+        #ifdef __QNX__
+        struct stat path_stat = {};
+        stat(pathname, &path_stat);
+        if(S_ISDIR(path_stat.st_mode)) flags |= O_DIRECTORY;
+        #endif
         fd = open(pathname, flags);
         if (fd < 0)
             return -1;
     }
 
 #ifndef MS_WINDOWS
