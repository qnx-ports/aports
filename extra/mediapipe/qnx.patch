diff --git a/setup.py b/setup.py
index e5422ffb6..1bc91841f 100644
--- a/setup.py
+++ b/setup.py
@@ -306,6 +306,7 @@ class BuildModules(build_ext.build_ext):
         '--compilation_mode=opt',
         '--copt=-DNDEBUG',
         '--action_env=PYTHON_BIN_PATH=' + _normalize_path(sys.executable),
+        '' if ((custom_args := os.environ.get('CUSTOM_BAZEL_ARGS')) is None) else custom_args,
         binary_graph_target,
     ] + GPU_OPTIONS
 
@@ -332,6 +333,7 @@ class GenerateMetadataSchema(build_ext.build_ext):
           'build',
           '--compilation_mode=opt',
           '--action_env=PYTHON_BIN_PATH=' + _normalize_path(sys.executable),
+          '' if ((custom_args := os.environ.get('CUSTOM_BAZEL_ARGS')) is None) else custom_args,
           '//mediapipe/tasks/metadata:' + target,
       ] + GPU_OPTIONS
 
@@ -422,6 +424,7 @@ class BuildExtension(build_ext.build_ext):
         '--copt=-DNDEBUG',
         '--keep_going',
         '--action_env=PYTHON_BIN_PATH=' + _normalize_path(sys.executable),
+        '' if ((custom_args := os.environ.get('CUSTOM_BAZEL_ARGS')) is None) else custom_args,
         str(ext.bazel_target + '.so'),
     ] + GPU_OPTIONS
 
