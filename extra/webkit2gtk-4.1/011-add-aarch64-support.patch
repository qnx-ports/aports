--- webkitgtk-2.51.1.orig/Source/JavaScriptCore/assembler/ARM64Assembler.h	2025-08-20 08:43:29.000000000 +0000
+++ webkitgtk-2.51.1/Source/JavaScriptCore/assembler/ARM64Assembler.h	2026-01-22 17:21:08.000000000 +0000
@@ -3750,7 +3750,7 @@
     
     unsigned debugOffset() { return m_buffer.debugOffset(); }
 
-#if OS(LINUX)
+#if OS(LINUX) | OS(QNX)
     static inline void linuxPageFlush(uintptr_t begin, uintptr_t end)
     {
         __builtin___clear_cache(reinterpret_cast<char*>(begin), reinterpret_cast<char*>(end));
@@ -3763,7 +3763,7 @@
         sys_icache_invalidate(code, size);
 #elif OS(FUCHSIA)
         zx_cache_flush(code, size, ZX_CACHE_FLUSH_INSN);
-#elif OS(LINUX)
+#elif OS(LINUX) | OS(QNX)
         size_t page = pageSize();
         uintptr_t current = reinterpret_cast<uintptr_t>(code);
         uintptr_t end = current + size;
--- webkitgtk-2.51.1.orig/Source/JavaScriptCore/offlineasm/arm64.rb	2025-09-22 12:00:15.000000000 +0000
+++ webkitgtk-2.51.1/Source/JavaScriptCore/offlineasm/arm64.rb	2026-01-22 17:22:57.000000000 +0000
@@ -1421,8 +1421,8 @@
             $asm.puts "ldr #{operands[1].arm64Operand(:quad)}, [#{operands[1].arm64Operand(:quad)}, #{operands[0].asmLabel}@GOTPAGEOFF]"
             $asm.putStr("#endif")
 
-            # On Linux, use ELF GOT relocation specifiers.
-            $asm.putStr("#elif OS(LINUX)")
+            # On Linux & QNX, use ELF GOT relocation specifiers.
+            $asm.putStr("#elif OS(LINUX) | OS(QNX)")
 
             $asm.puts "adrp #{operands[1].arm64Operand(:quad)}, :got:#{operands[0].asmLabel}"
             $asm.putStr("#if CPU(ADDRESS32)")
--- a/Source/cmake/WebKitCommon.cmake
+++ b/Source/cmake/WebKitCommon.cmake
@@ -99,6 +99,12 @@
     else ()
         string(TOLOWER ${CMAKE_SYSTEM_PROCESSOR} LOWERCASE_CMAKE_SYSTEM_PROCESSOR)
     endif ()
+    
+    # QNX reports "unknown" as CMAKE_SYSTEM_PROCESSOR, use uname instead
+    if (CMAKE_SYSTEM_NAME MATCHES "QNX" AND LOWERCASE_CMAKE_SYSTEM_PROCESSOR STREQUAL "unknown")
+        execute_process(COMMAND uname -m OUTPUT_VARIABLE QNX_UNAME_PROCESSOR OUTPUT_STRIP_TRAILING_WHITESPACE)
+        string(TOLOWER ${QNX_UNAME_PROCESSOR} LOWERCASE_CMAKE_SYSTEM_PROCESSOR)
+    endif ()
     if (LOWERCASE_CMAKE_SYSTEM_PROCESSOR MATCHES "(^aarch64|^arm64|^cortex-?[am][2-7][2-8])")
          if (FORCE_32BIT)
             set(WTF_CPU_ARM 1)
@@ -111,7 +117,7 @@
         set(WTF_CPU_MIPS64 1)
     elseif (LOWERCASE_CMAKE_SYSTEM_PROCESSOR MATCHES "^mips")
         set(WTF_CPU_MIPS 1)
-    elseif (LOWERCASE_CMAKE_SYSTEM_PROCESSOR MATCHES "(x64|x86_64|amd64)")
+    elseif (LOWERCASE_CMAKE_SYSTEM_PROCESSOR MATCHES "(x64|x86_64|amd64|x86pc)")
         # FORCE_32BIT is set in the build script when --32-bit is passed
         # on a Linux/intel 64bit host. This allows us to produce 32bit
         # binaries without setting the build up as a crosscompilation,
--- a/Source/WebKit/NetworkProcess/soup/NetworkProcessSoup.cpp	2026-02-06 14:50:34.000000000 +0000
+++ b/Source/WebKit/NetworkProcess/soup/NetworkProcessSoup.cpp	2026-02-06 14:51:26.000000000 +0000
@@ -125,6 +125,7 @@
void NetworkProcess::platformInitializeNetworkProcess(const NetworkProcessCreationParameters& parameters)
{
+#ifndef __QNX__
     GRefPtr<GResolver> cachedResolver = adoptGRef(webkitCachedResolverNew(adoptGRef(g_resolver_get_default())));
     g_resolver_set_default(cachedResolver.get());
@@ -132,7 +133,7 @@
         auto overridingResolver = adoptGRef(webkitOverridingResolverNew(cachedResolver.get(), parameters.localhostAliasesForTesting));
         g_resolver_set_default(overridingResolver.get());
     }
-
+#endif
     m_cacheOptions = { NetworkCache::CacheOption::RegisterNotify };
     // FIXME: NetworkProcess probably does not have session at this point.
     forEachNetworkSession([&](NetworkSession& session) {
