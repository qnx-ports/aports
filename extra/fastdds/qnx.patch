--- Fast-DDS-2.14.4/CMakeLists.txt	2026-03-10 06:42:44.000000000 +0000
+++ Fast-DDS-2.14.4_with_qnx_changes/CMakeLists.txt	2026-03-02 06:27:19.000000000 +0000
@@ -257,6 +257,7 @@
 option(SECURITY "Activate security" OFF)
 
 if(QNX)
+    find_package(OpenSSL REQUIRED)
     set(OPENSSL_FOUND 1)
 else()
     if(SECURITY)
@@ -297,6 +298,10 @@
     set(OPENSSL_LIBRARIES ssl crypto)
 endif()
 
+if(QNX AND ${CMAKE_CXX_COMPILER_VERSION} VERSION_GREATER_EQUAL 12.2.0)
+    link_libraries(fsnotify)
+endif()
+
 option(SQLITE3_SUPPORT "Activate SQLITE3 support" ON)
 
 ###############################################################################
--- Fast-DDS-2.14.4/src/cpp/rtps/reader/WriterProxy.cpp	2026-03-10 06:42:44.000000000 +0000
+++ Fast-DDS-2.14.4_with_qnx_changes/src/cpp/rtps/reader/WriterProxy.cpp	2026-03-02 06:27:19.000000000 +0000
@@ -34,7 +34,7 @@
 #include "rtps/RTPSDomainImpl.hpp"
 #include "utils/collections/node_size_helpers.hpp"
 
-#if !defined(NDEBUG) && !defined(ANDROID) && defined(FASTRTPS_SOURCE) && defined(__unix__)
+#if !defined(NDEBUG) && !defined(ANDROID) && defined(FASTRTPS_SOURCE) && defined(__unix__) && !defined(__QNX__)
 #define SHOULD_DEBUG_LINUX
 #endif // SHOULD_DEBUG_LINUX
 
--- Fast-DDS-2.14.4/src/cpp/utils/threading/threading_pthread.ipp	2026-03-10 06:42:44.000000000 +0000
+++ Fast-DDS-2.14.4_with_qnx_changes/src/cpp/utils/threading/threading_pthread.ipp	2026-03-02 06:27:19.000000000 +0000
@@ -18,8 +18,10 @@
 #include <limits>
 
 #include <pthread.h>
-#include <sys/resource.h>
+#ifndef __QNX__ // not available on QNX
 #include <sys/sysinfo.h>
+#endif
+#include <sys/resource.h>
 #include <sys/time.h>
 #include <sys/types.h>
 
@@ -26,3 +28,4 @@
+
 #include <fastdds/dds/log/Log.hpp>
 #include <fastdds/rtps/attributes/ThreadSettings.hpp>
 
@@ -95,9 +98,8 @@
     // Set Scheduler Class and Priority
     //
 
-    if ((sched_class == SCHED_OTHER) ||
-            (sched_class == SCHED_BATCH) ||
-            (sched_class == SCHED_IDLE))
+     if (sched_class == SCHED_OTHER)
+
     {
         //
         // BATCH and IDLE do not have explicit priority values.
@@ -145,6 +147,7 @@
         const char* thread_name,
         uint64_t affinity_mask)
 {
+#ifndef __QNX__ // not available on QNX
     int a;
     int result;
     int cpu_count;
@@ -195,6 +198,7 @@
         EPROSIMA_LOG_ERROR(SYSTEM, "Problem to set affinity of thread with id [" << self_tid << "," << thread_name << "] to value " << affinity_mask << ". Error '" << strerror(
                     result) << "'");
     }
+#endif
 }
 
 void apply_thread_settings_to_current_thread(
--- Fast-DDS-2.14.4/test/CMakeLists.txt	2026-03-10 06:42:44.000000000 +0000
+++ Fast-DDS-2.14.4_with_qnx_changes/test/CMakeLists.txt	2026-03-02 06:27:19.000000000 +0000
@@ -61,7 +61,7 @@
     add_subdirectory(dds/communication/security)
     add_subdirectory(dds/discovery)
 
-    if(UNIX AND NOT APPLE AND STRICT_REALTIME)
+    if(UNIX AND NOT APPLE AND NOT QNX AND STRICT_REALTIME)
         add_subdirectory(realtime)
     endif()
 endif()
