QNX Vulkan compatibility for the self-hosted APK build.

- QNX 8.0 Vulkan-Hpp headers do not expose VK_EXT_layer_settings C++ wrapper
  types, so skip optional layer-settings setup on QNX.
- QNX glslc can return successfully without producing output when several
  shader compiles run concurrently inside one generator process, so run one
  glslc child at a time on QNX.
- QNX shaderc/SPIRV-Tools rejects optimized output for generated llama.cpp
  shaders with VUID-StandaloneSpirv-None-10684, so keep upstream -O behavior
  for other platforms and skip it only on QNX.
- QNX glslc reports cooperative-matrix support, but generated cooperative
  matrix shaders can complete without writing output, so disable those optional
  variants on QNX and filter the cm shader sources out of the generated source
  list.
- QNX glslc intermittently exits successfully without writing output for some
  shaders, so retry the compiler command a small number of times before failing
  the build.
- Convert shader generation failures into exceptions observed via future::get()
  so failed or missing shader outputs stop the build deterministically.
---
diff --git a/ggml/src/ggml-vulkan/CMakeLists.txt b/ggml/src/ggml-vulkan/CMakeLists.txt
index 715a263..19be91a 100644
--- a/ggml/src/ggml-vulkan/CMakeLists.txt
+++ b/ggml/src/ggml-vulkan/CMakeLists.txt
@@ -31,6 +31,14 @@ endfunction()
 #  TEST_SHADER_FILE - Path to the test shader file
 #  RESULT_VARIABLE - Name of the variable to set (ON/OFF) based on test result
 function(test_shader_extension_support EXTENSION_NAME TEST_SHADER_FILE RESULT_VARIABLE)
+    if (CMAKE_SYSTEM_NAME MATCHES "QNX" AND
+            (EXTENSION_NAME STREQUAL "GL_KHR_cooperative_matrix" OR
+             EXTENSION_NAME STREQUAL "GL_NV_cooperative_matrix2"))
+        message(STATUS "${EXTENSION_NAME} disabled on QNX: generated cooperative matrix shaders do not complete reliably")
+        set(${RESULT_VARIABLE} OFF PARENT_SCOPE)
+        return()
+    endif()
+
     execute_process(
         COMMAND ${Vulkan_GLSLC_EXECUTABLE} -o - -fshader-stage=compute --target-env=vulkan1.3 "${TEST_SHADER_FILE}"
         OUTPUT_VARIABLE glslc_output
@@ -175,6 +183,12 @@ if (Vulkan_FOUND)
 
     file(GLOB _ggml_vk_shader_files CONFIGURE_DEPENDS "${_ggml_vk_input_dir}/*.comp")
 
+    if (CMAKE_SYSTEM_NAME MATCHES "QNX")
+        # Cooperative matrix shader variants are disabled above on QNX, so do
+        # not generate no-op cm translation units or depfile warnings.
+        list(FILTER _ggml_vk_shader_files EXCLUDE REGEX ".*/(flash_attn_cm[12]|mul_mm_cm[12])\\.comp$")
+    endif()
+
     # Because external projects do not provide source-level tracking,
     # the vulkan-shaders-gen sources need to be explicitly added to
     # ensure that changes will cascade into shader re-generation.
diff --git a/ggml/src/ggml-vulkan/ggml-vulkan.cpp b/ggml/src/ggml-vulkan/ggml-vulkan.cpp
index c2f1883..697778b 100644
--- a/ggml/src/ggml-vulkan/ggml-vulkan.cpp
+++ b/ggml/src/ggml-vulkan/ggml-vulkan.cpp
@@ -5885,7 +5885,12 @@ static void ggml_vk_instance_init() {
     vk::ApplicationInfo app_info{ "ggml-vulkan", 1, nullptr, 0, api_version };
 
     const std::vector<vk::ExtensionProperties> instance_extensions = vk::enumerateInstanceExtensionProperties();
+#if defined(__QNX__)
+    // QNX 8.0 Vulkan-Hpp headers do not expose VK_EXT_layer_settings types.
+    const bool layer_settings = false;
+#else
     const bool layer_settings = ggml_vk_instance_layer_settings_available();
+#endif
 #ifdef __APPLE__
     const bool portability_enumeration_ext = ggml_vk_instance_portability_enumeration_ext_available(instance_extensions);
 #endif
@@ -5907,6 +5912,9 @@ static void ggml_vk_instance_init() {
     if (debug_utils_ext) {
         extensions.push_back("VK_EXT_debug_utils");
     }
+#if defined(__QNX__)
+    vk::InstanceCreateInfo instance_create_info(vk::InstanceCreateFlags{}, &app_info, layers, extensions);
+#else
     VkBool32 enable_best_practice = layer_settings;
     std::vector<vk::LayerSettingEXT> settings = {
         {
@@ -5919,6 +5927,7 @@ static void ggml_vk_instance_init() {
     };
     vk::LayerSettingsCreateInfoEXT layer_setting_info(settings);
     vk::InstanceCreateInfo instance_create_info(vk::InstanceCreateFlags{}, &app_info, layers, extensions, &layer_setting_info);
+#endif
 #ifdef __APPLE__
     if (portability_enumeration_ext) {
         instance_create_info.flags |= vk::InstanceCreateFlagBits::eEnumeratePortabilityKHR;
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp b/ggml/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp
index 6f2a929..b0e0bb3 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp
@@ -317,7 +317,14 @@ using compile_count_guard = std::unique_ptr<uint32_t, decltype(&decrement_compil
 compile_count_guard acquire_compile_slot() {
     // wait until fewer than N compiles are in progress.
     // 16 is an arbitrary limit, the goal is to avoid "failed to create pipe" errors.
+#ifdef __QNX__
+    // QNX glslc can return successfully without producing output when several
+    // shader compiles run inside one generator process. Launch one glslc child
+    // at a time and let the build fail if any expected output is still missing.
+    uint32_t N = 1;
+#else
     uint32_t N = std::max(1u, std::min(16u, std::thread::hardware_concurrency()));
+#endif
     std::unique_lock<std::mutex> guard(compile_count_mutex);
     compile_count_cond.wait(guard, [N] { return compile_count < N; });
     compile_count++;
@@ -336,9 +343,14 @@ void string_to_spv_func(std::string name, std::string in_path, std::string out_p
     // disable spirv-opt for coopmat shaders for https://github.com/ggml-org/llama.cpp/issues/10734
     // disable spirv-opt for bf16 shaders for https://github.com/ggml-org/llama.cpp/issues/15344
     // disable spirv-opt for rope shaders for https://github.com/ggml-org/llama.cpp/issues/16860
+#ifdef __QNX__
+    // QNX shaderc/SPIRV-Tools rejects optimized output for generated llama.cpp shaders
+    // with VUID-StandaloneSpirv-None-10684. Keep upstream optimization elsewhere.
+#else
     if (!coopmat && name.find("bf16") == std::string::npos && name.find("rope") == std::string::npos) {
         cmd.push_back("-O");
     }
+#endif
 
     if (dep_file) {
         cmd.push_back("-MD");
@@ -371,14 +383,40 @@ void string_to_spv_func(std::string name, std::string in_path, std::string out_p
         // }
         // std::cout << std::endl;
 
-        execute_command(cmd, stdout_str, stderr_str);
+        std::string spv;
+#ifdef __QNX__
+        constexpr int max_attempts = 3;
+#else
+        constexpr int max_attempts = 1;
+#endif
+        for (int attempt = 1; attempt <= max_attempts; ++attempt) {
+            stdout_str.clear();
+            stderr_str.clear();
+            execute_command(cmd, stdout_str, stderr_str);
+            if (!stderr_str.empty()) {
+                break;
+            }
+            spv = read_binary_file(out_path, true);
+            if (!spv.empty()) {
+                break;
+            }
+#ifdef __QNX__
+            if (attempt != max_attempts) {
+                std::cerr << "glslc produced empty output for " << name << ", retrying" << std::endl;
+            }
+#endif
+        }
         if (!stderr_str.empty()) {
             std::cerr << "cannot compile " << name << "\n\n";
             for (const auto& part : cmd) {
                 std::cerr << part << " ";
             }
             std::cerr << "\n\n" << stderr_str << std::endl;
-            return;
+            throw std::runtime_error("glslc failed for " + name);
+        }
+
+        if (spv.empty()) {
+            throw std::runtime_error("glslc did not produce shader output for " + name + ": " + out_path);
         }
 
         if (dep_file) {
@@ -397,6 +435,7 @@ void string_to_spv_func(std::string name, std::string in_path, std::string out_p
         shader_fnames.push_back(std::make_pair(name, out_path));
     } catch (const std::exception& e) {
         std::cerr << "Error executing command for " << name << ": " << e.what() << std::endl;
+        throw;
     }
 }
 
@@ -1026,7 +1065,7 @@ void process_shaders() {
     string_to_spv("topk_moe_f32", "topk_moe.comp", {});
 
     for (auto &c : compiles) {
-        c.wait();
+        c.get();
     }
 }
 
