diff --git a/realtime_tools/CMakeLists.txt b/realtime_tools/CMakeLists.txt
index a9b5696..6f8b82a 100644
--- a/realtime_tools/CMakeLists.txt
+++ b/realtime_tools/CMakeLists.txt
@@ -46,7 +46,7 @@ target_include_directories(realtime_tools PUBLIC
   $<INSTALL_INTERFACE:include/realtime_tools>
 )
 target_link_libraries(realtime_tools PUBLIC rclcpp::rclcpp rclcpp_action::rclcpp_action rcpputils::rcpputils Threads::Threads Boost::boost)
-if(UNIX AND NOT APPLE)
+if(UNIX AND NOT APPLE AND NOT CMAKE_SYSTEM_NAME STREQUAL "QNX")
   target_link_libraries(realtime_tools PUBLIC cap)
 endif()
 
@@ -60,7 +60,7 @@ target_include_directories(thread_priority PUBLIC
   $<INSTALL_INTERFACE:include/realtime_tools>
 )
 target_link_libraries(thread_priority PUBLIC rclcpp::rclcpp rclcpp_action::rclcpp_action rcpputils::rcpputils Threads::Threads)
-if(UNIX AND NOT APPLE)
+if(UNIX AND NOT APPLE AND NOT CMAKE_SYSTEM_NAME STREQUAL "QNX")
   target_link_libraries(thread_priority PUBLIC cap)
 endif()
 
diff --git a/realtime_tools/src/realtime_helpers.cpp b/realtime_tools/src/realtime_helpers.cpp
index a13792a..2bab01e 100644
--- a/realtime_tools/src/realtime_helpers.cpp
+++ b/realtime_tools/src/realtime_helpers.cpp
@@ -32,7 +32,7 @@
 #include <windows.h>
 #else
 #include <sched.h>
-#if defined(__unix__)
+#if defined(__unix__) && !defined(__QNX__)
 #include <sys/capability.h>
 #endif
 #include <sys/mman.h>
@@ -48,8 +48,8 @@ namespace realtime_tools
 {
 bool has_realtime_kernel()
 {
-#ifdef _WIN32
-  std::cerr << "Realtime kernel detection is not supported on Windows." << std::endl;
+#ifdef defined(_WIN32) || defined(__QNX__)
+  std::cerr << "Realtime kernel detection is not supported on Windows or QNX." << std::endl;
   return false;
 #else
   std::ifstream realtime_file("/sys/kernel/realtime", std::ios::in);
@@ -107,8 +107,8 @@ bool lock_memory(std::string & message)
 
 std::pair<bool, std::string> lock_memory()
 {
-#if defined(_WIN32) || defined(__APPLE__)
-  return {false, "Memory locking is not supported on Windows or macOS."};
+#if defined(_WIN32) || defined(__APPLE__) || defined(__QNX__)
+  return {false, "Memory locking is not supported on Windows, macOS or QNX."};
 #else
   auto is_capable = [](cap_value_t v) -> bool {
     bool rc = false;
@@ -158,8 +158,8 @@ std::pair<bool, std::string> set_thread_affinity(
   NATIVE_THREAD_HANDLE thread, const std::vector<int> & cores)
 {
   std::string message;
-#if defined(_WIN32) || defined(__APPLE__)
-  message = "Thread affinity is not supported on Windows or macOS.";
+#if defined(_WIN32) || defined(__APPLE__) || defined(__QNX__)
+  message = "Thread affinity is not supported on Windows, macOS or QNX.";
   return std::make_pair(false, message);
 #else
   auto set_affinity_result_message = [](int result, std::string & msg) -> bool {
