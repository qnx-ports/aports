--- a/src/user/user_mesh.cc
+++ b/src/user/user_mesh.cc
@@ -18,6 +18,14 @@
 #include <climits>
 #include <cmath>
 #include <csetjmp>
+
+#if defined(__QNX__)&& !defined(setjmp)
+// libc++'s <csetjmp> undefines the setjmp macro on QNX (it only
+// re-exposes ::longjmp), but QNX libc exports _setjmp/sigsetjmp, not a
+// plain setjmp symbol, so unqualified setjmp() calls below fail to link.
+// Restore the standard setjmp->sigsetjmp redirect locally.
+#define setjmp(buf) sigsetjmp(buf, 1)
+#endif
 #include <cstddef>
 #include <cstdio>
 #include <cstring>
--- a/src/user/user_model.cc
+++ b/src/user/user_model.cc
@@ -19,6 +19,14 @@
 #include <chrono>
 #include <cmath>
 #include <csetjmp>
+
+#if defined(__QNX__)&& !defined(setjmp)
+// libc++'s <csetjmp> undefines the setjmp macro on QNX (it only
+// re-exposes ::longjmp), but QNX libc exports _setjmp/sigsetjmp, not a
+// plain setjmp symbol, so unqualified setjmp() calls below fail to link.
+// Restore the standard setjmp->sigsetjmp redirect locally.
+#define setjmp(buf) sigsetjmp(buf, 1)
+#endif
 #include <cstdint>
 #include <cstdio>
 #include <cstdlib>
--- a/test/engine/engine_util_errmem_test.cc
+++ b/test/engine/engine_util_errmem_test.cc
@@ -15,6 +15,14 @@
 // Tests for engine/engine_util_errmem.c.
 
 #include <csetjmp>
+
+#if defined(__QNX__)&& !defined(setjmp)
+// libc++'s <csetjmp> undefines the setjmp macro on QNX (it only
+// re-exposes ::longjmp), but QNX libc exports _setjmp/sigsetjmp, not a
+// plain setjmp symbol, so unqualified setjmp() calls below fail to link.
+// Restore the standard setjmp->sigsetjmp redirect locally.
+#define setjmp(buf) sigsetjmp(buf, 1)
+#endif
 #include <cstdio>
 #include <cstring>
 #include <string>
